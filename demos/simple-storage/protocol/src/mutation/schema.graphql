#import { Mutation } into Ethereum from "w3://ens/ethereum.web3api.eth"

type Mutation {
  setData(
    address: String!
    value: CustomType!
  ): CustomType!

  deployContract: String!
}

type CustomType {
  prop: String!
}

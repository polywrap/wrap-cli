#import { Mutation } into Ethereum from "w3://ens/ethereum.web3api.eth"

type Mutation {
  setData(
    options: Options!
  ): SetDataResult!

  deployContract: String!
}

type Options {
  address: String!
  value: UInt32!
}

type SetDataResult {
  value: UInt32!
  txRecript: String!
}

mutation setRecord(
  $domain: String!, $owner: String!, $resolverAddress: String!, $ttl: String!, $registryAddress: String!
) {
  setRecord(domain: $domain, owner: $owner, resolverAddress: $resolverAddress, ttl: $ttl, registryAddress: $registryAddress)
}
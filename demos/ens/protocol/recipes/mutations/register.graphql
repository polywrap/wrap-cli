mutation {
  registerDomain(
    domain: $domain
    registrarAddress: $registrarAddress
    registryAddress: $registryAddress
    owner: $owner
    connection: {
      networkNameOrChainId: $network
    }
  )
}
mutation RegisterDomain($domain: String!, $registrarAddress: String!, $registryAddress: String!, $owner: String!) {
  registerDomain(
    domain: $domain,
    registrarAddress: $registrarAddress,
    registryAddress: $registryAddress,
    owner: $owner
  )
}
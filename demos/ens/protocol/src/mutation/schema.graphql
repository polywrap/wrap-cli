#import { Mutation } into Ethereum from "w3://ens/ethereum.web3api.eth"

type Mutation {
  setData(address: String!, value: UInt32!): String!
  setResolver(contractAddress: String!, resolverAddress: String!): String!
}

TODO:
- move `plugin codegen` and `app codegen` to just `codegen`
- move `--codegen-dir`, `--publish-dir`, and `--client-config` into manifest file? probably good for a new PR

```
polywrap codegen
polywrap build
polywrap deploy
polywrap infra
polywrap create
polywrap run
```

TODO:
- move 

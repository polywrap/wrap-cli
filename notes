1. remove sanitize env
2. eager load environment
3. env must be defined in method arguments
4. don't allow users to name arguments "env" w/o `type Env`
5. make sure path for lazy loading exists (it should)
6. update app codegen

Nice To Haves
1. only fail when env property is requested, but not found

Parts:
- Parse
- Compose
- Bindings
  - Wasm
    - AssemblyScript
    - Rust
  - Plugin
    - TypeScript
  - App
    - TypeScript
- Client Tests
  - Rust
  - AssemblyScript
  - Plugin Tests
  - App Codegen?

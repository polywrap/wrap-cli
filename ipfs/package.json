{
  "name": "@web3api/ipfs",
  "version": "0.0.1-prealpha.1",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "w3 build",
    "deploy": "w3 build --ipfs http://127.0.0.1:5001 --test-ens ipfs.eth",
    "test": "w3 query ./recipes/e2e.json",
    "test:ci": "yarn test:env:up && yarn deploy && yarn test && yarn test:env:down",
    "test:env:up": "w3 test-env up && jsipfs add ./recipes/test.txt --api http://localhost:5001",
    "test:env:down": "w3 test-env down"
  },
  "devDependencies": {
    "@web3api/cli": "0.0.1-prealpha.4",
    "@web3api/ethereum-plugin-js": "0.0.1-prealpha.4",
    "@web3api/wasm-as": "0.0.1-prealpha.4",
    "ethers": "5.0.8",
    "js-yaml": "3.14.0",
    "ipfs": "0.54.2"
  }
}

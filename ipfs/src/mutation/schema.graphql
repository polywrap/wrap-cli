#import { Mutation, ResponseType } into Http from "w3://ens/http.web3api.eth"

type Mutation {
  addFile(ipfsUrl: String!, fileName: String!, data: Bytes!): AddResult!
}

type AddResult {
  name: String!
  hash: String!
  size: String!
}
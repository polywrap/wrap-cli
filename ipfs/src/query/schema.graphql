#import { Query, ResponseType } into Http from "w3://ens/http.web3api.eth"

type Query {
  catToString(ipfsUrl: String!, cid: String!): String!
  catFile(ipfsUrl: String!, cid: String!): Bytes!

  tryResolveUri(ipfsUrl: String!, authority: String!, path: String!): String
}

{
  "name": "@web3api/manifest-schema",
  "version": "0.0.1-alpha.1",
  "license": "MIT",
  "main": "build/0-0-1.json",
  "scripts": {
    "build": "rm -rf build && mkdir build && cp versions/0-0-1.json build"
  }
}

{
  "properties": {
    "config": {
      "bundledScriptPath": {
        "description": "Path to the bundled JS file to build the wrap from",
        "type": "string",
        "required": true
      }
    }
  }
}
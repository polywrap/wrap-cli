name: test-workflow
format: "0.1"
jobs:
  sampleMethod:
    steps:
      - uri: fs/build
        method: sampleMethod
        not_accepted_field: not_accepted_field
        args:
          arg: "polywrap"

#import { Mutation } into Ethereum from "w3://ens/ethereum.web3api.eth"

type Mutation {
  setData(
    address: String!
    value: Int!
    bytesValue: Bytes!
  ): String!

  deployContract: String!
}

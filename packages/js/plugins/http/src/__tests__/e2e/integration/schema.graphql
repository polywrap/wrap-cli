#import { Module, Request, Response } into HTTP from "w3://ens/http.web3api.eth"

type Module {
  get(
    url: String!
    request: HTTP_Request
  ): HTTP_Response

  post(
    url: String!
    request: HTTP_Request
  ): HTTP_Response
}

#import { Query, MaybeUriOrManifest } into UriResolver from "ens/uri-resolver.core.web3api.eth"
#import { Query } into Ethereum from "ens/ethereum.web3api.eth"

type Module implements UriResolver_Query { }

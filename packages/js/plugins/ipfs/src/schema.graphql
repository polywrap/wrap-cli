#implement { Query } from "w3/api-resolver"
#implement { Query } from "w3/uri-resolver"

type Query {
  catFile(cid: String!): Bytes!
}

type Mutation {
  """TODO: Allow for custom type CID"""
  addFile(data: Bytes!): AddResult!
}

type AddResult {
  path: String!
  cid: String!
}

# TODO: add declaration for "implement"ing uri-resolver & api-resolver

type Query {
  catFile(cid: String!): Bytes!
}

type Mutation {
  """TODO: Allow for custom type CID"""
  addFile(data: Bytes!): AddResult!
}

type AddResult {
  path: String!
  cid: String!
}

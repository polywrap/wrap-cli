#implement { Query } from "w3://ens/api-resolver.core.web3api.eth"
#implement { Query } from "w3://ens/uri-resolver.core.web3api.eth"

type Query {
  catFile(cid: String!): Bytes!
}

type Mutation {
  """TODO: Allow for custom type CID"""
  addFile(data: Bytes!): AddResult!
}

type AddResult {
  path: String!
  cid: String!
}

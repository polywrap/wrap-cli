#import { Module, Callback } into WS from "wrap://ens/ws.polywrap.eth"

type Module {
  send(
    url: String!
    message: String!
  ): Boolean!

  subscribe(
    url: String!,
    callback: WS_Callback!
  ): Boolean!

  get(
    url: String!,
    timeout: Int!
  ): [String!]!
}

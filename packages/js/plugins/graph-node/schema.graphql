type Query {
  #eslint-disable-next-line @graphql-eslint/avoid-typename-prefix
  querySubgraph(subgraphId: String!, query: String!): String! #JSON!
  #TODO: support JSON type as base type?
  #I think this would be helpful for dynamic data
}

export declare function up(
  quiet: boolean,
  configFilePath: string,
  ci: boolean
): Promise<void>;
export declare function down(
  quiet: boolean,
  configFilePath: string,
  ci: boolean
): Promise<void>;

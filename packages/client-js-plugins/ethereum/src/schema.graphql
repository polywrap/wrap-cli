type Query {
  callView(
    address: String!
    method: String!
    args: [String!]!
  ): String!
}

type Mutation {
  sendTransaction(
    address: String!
    method: String!
    args: [String!]!
  ): String!

  deployContract(
    abi: String!
    bytecode: String!
  ): String!
}

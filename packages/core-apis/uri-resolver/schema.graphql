type Query {
  supportedUriAuthority(
    authority: String!
  ): Boolean!

  tryResolveUriPath(
    path: String!
  ): MaybeUriOrManifest!
}

type MaybeUriOrManifest {
  uri: String
  manifest: String
}

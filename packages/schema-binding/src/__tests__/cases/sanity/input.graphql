"""TODO: Use the graphql-composer to include the header"""
scalar UInt
scalar UInt8
scalar UInt16
scalar UInt32
scalar UInt64
scalar Int
scalar Int8
scalar Int16
scalar Int32
scalar Int64

directive @import(
  namespace: String!
  uri: String!
  types: [String!]!
) on OBJECT

directive @imported_type(
  namespace: String!
  type: String!
) on OBJECT

type CustomType {
  str: String!
  optStr: String
  u: UInt!
  optU: UInt
  u8: UInt8!
  u16: UInt16!
  u32: UInt32!
  u64: UInt64!
  i: Int!
  i8: Int8!
  i16: Int16!
  i32: Int32!
  i64: Int64!
  uArray: [UInt!]!
  uOptArray: [UInt!]
  optUOptArray: [UInt]
  optStrOptArray: [String]
  uArrayArray: [[UInt!]!]!
  uOptArrayOptArray: [[UInt64]]!
  uArrayOptArrayArray: [[[UInt64!]!]]!
  crazyArray: [[[[UInt64!]]!]]
}

type AnotherType {
  prop: String
}

@import(
  namespace: "Imported",
  uri: "imported.uri.eth",
  types: ["Query"]
)
type Query {
  queryMethod(
    arg: String!
  ): Int!
}

@imported_type(
  namespace: "Imported",
  type: "Query"
)
type ImportedQuery {
  importedMethod(
    str: String!
    optStr: String
    u: UInt!
    optU: UInt
    uArrayArray: [[UInt]]!
  ): String!
  """TODO: Support object types as argument & return values"""
}

import { Schema } from "./";

export type BaseTypes = "String" | "UInt" | "Array"

export function validateSchema(schema: Schema): Error[] {
  // TODO: ./validators/index.ts runValidators
  return []
}

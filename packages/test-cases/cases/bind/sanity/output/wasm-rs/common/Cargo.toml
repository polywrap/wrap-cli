[package]
name = "common"
version = "0.0.1"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
polywrap-wasm-rs = { version = "0.0.1-prealpha.75" }
serde = { version = "1.0", features = ["derive"] }

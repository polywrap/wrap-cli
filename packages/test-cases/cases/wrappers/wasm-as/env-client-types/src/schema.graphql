type Module {
  sanitizeEnv(env: ClientEnv!): Env!
  environment(arg: String!): Env
}

type Env {
  str: String!
  optStr: String
  defStr: String!
}

type ClientEnv {
  str: String!
  optStr: String
}

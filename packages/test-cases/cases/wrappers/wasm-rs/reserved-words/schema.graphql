#import { Module, else, Box, while } into TestImport from "wrap://ens/reserved-words.polytest.eth"

type Module {
  if(
    if: else!
  ): else!

  for(
    in: while!
  ): Box!
}

type else {
  else: String!
}

type Box {
  box: String!
}

enum while {
  for,
  in,
}
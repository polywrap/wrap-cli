type Query {
  sanitizeQueryEnv(env: QueryClientEnv!): QueryEnv!
  environment(arg: String!): QueryEnv
}

type QueryEnv {
  str: String!
  optStr: String
  defStr: String!
}

type QueryClientEnv {
  str: String!
  optStr: String
}

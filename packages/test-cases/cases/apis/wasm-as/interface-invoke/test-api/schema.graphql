#import { Module, InterfaceType } into Interface from "w3://ens/interface.eth"
#use { getImplementations } for Interface

type Module {
  moduleMethod(
    arg: ImplementationType!
  ): ImplementationType!
}

type ImplementationType implements Interface_InterfaceType {
  str: String!
}
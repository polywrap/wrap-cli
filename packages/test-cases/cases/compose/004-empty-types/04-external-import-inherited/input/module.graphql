#import { ImportedBaseType } into Base from "base.eth"
#import { ImportedDerivedType } into Derived from "derived.eth"

type Module {
  method1: Derived_ImportedDerivedType
  method2: CustomType
}

type CustomType implements Base_ImportedBaseType {
}

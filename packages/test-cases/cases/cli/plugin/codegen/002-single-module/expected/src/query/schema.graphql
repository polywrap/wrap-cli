#import { Query } into Ethereum from "ens/ethereum.web3api.eth"

type Module {
  method(
    str: String!
    optStr: String
  ): Object!
}

type Object {
  u: UInt!
  array: [Boolean!]!
  bytes: Bytes
}

type Env {
  arg1: String!
}

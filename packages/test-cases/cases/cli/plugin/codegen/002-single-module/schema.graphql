#import { Module } into Ethereum from "ens/ethereum.polywrap.eth"

type Module {
  method(
    str: String!
    optStr: String
  ): Object!
}

type Object {
  u: UInt!
  array: [Boolean!]!
  bytes: Bytes
}

type Env {
  arg1: String!
}

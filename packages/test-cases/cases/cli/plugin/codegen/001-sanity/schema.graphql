#import { Module } into Ethereum from "ens/ethereum.polywrap.eth"

type Module {
    methodOne(
        str: String!
        optStr: String
    ): Object!

    methodTwo(
        arg: UInt32!
    ): String!
}

type Object {
    u: UInt!
    array: [Boolean!]!
    bytes: Bytes
}

type Env {
    arg1: String!
}

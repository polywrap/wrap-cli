#import { Module } into Ethereum from "ens/wraps.eth:ethereum@1.1.0"

type Module {
    methodOne(
        str: String!
        optStr: String
    ): Object!

    methodTwo(
        arg: UInt32!
    ): String!
}

type Object {
    u: UInt!
    array: [Boolean!]!
    bytes: Bytes
}

type Env {
    arg1: String!
}

#import { Module } into Ethereum from "ens/wraps.eth:ethereum@1.1.0"

type Module {
    method(
        str: String!
        optStr: String
    ): Object!
}

type Object {
    u: UInt!
    array: [Boolean!]!
    bytes: Bytes
}

"""Test Comment"""
type Object2 {
    """Test Comment"""
    u: UInt!
    """Test Comment"""
    array: [Boolean!]!
    """Test Comment"""
    bytes: Bytes
}

type Object3 {
    u: UInt!
    array: [Boolean!]!
    bytes: Bytes
}

enum test {
    ARG1,
    ARG2
}

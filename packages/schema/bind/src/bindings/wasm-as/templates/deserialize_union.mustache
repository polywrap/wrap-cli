{{#required}}
const union = Types.{{type}}.read(reader);
{{/required}}
{{^required}}
let union: {{#toWasm}}{{toGraphQLType}}{{/toWasm}} = null;
if (!reader.isNextNil()) {
  union = Types.{{type}}.read(reader);
}
{{/required}}

{{#required}}
const object = Objects.{{type}}.fromBuffer(reader.readBytes());
{{/required}}
{{^required}}
const bytes = reader.readNullableBytes();
var object: {{#toWasm}}{{toGraphQLType}}{{/toWasm}} = null;
if (bytes) {
  object = Objects.{{type}}.fromBuffer(bytes);
}
{{/required}}

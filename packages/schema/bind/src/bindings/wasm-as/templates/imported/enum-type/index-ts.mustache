export enum {{type}} {
  {{#constants}}
  {{.}},
  {{/constants}}
}

export function match{{type}}ByValue(value: i32): {{type}} {
  {{#constants}}
  if ({{type}}.{{.}} == value) {
    return {{type}}.{{.}};
  }
  {{/constants}}

  throw new Error("Invalid value for enum '{{type}}'");
}

export function match{{type}}ByKey(key: string): {{type}} {
  {{#constants}}
  if (key == "{{.}}") {
    return {{type}}.{{.}};
  }
  {{/constants}}

  throw new Error("Invalid key for enum '{{type}}'");
}

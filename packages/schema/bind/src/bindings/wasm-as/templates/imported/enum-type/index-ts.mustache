export enum {{type}} {
  {{#constants}}
  {{.}},
  {{/constants}}
}

export function match{{type}}ByValue(value: u32): {{type}} {
  switch (value) {
    {{#constants}}
    case {{type}}.{{.}}: return {{type}}.{{.}};
    {{/constants}}
    default:
      throw new Error("Invalid value for enum '{{type}}': " + value.toString());
  }
}

export function match{{type}}ByKey(key: string): {{type}} {
  {{#constants}}
  if (key == "{{.}}") {
    return {{type}}.{{.}};
  }
  {{/constants}}

  throw new Error("Invalid key for enum '{{type}}': " + key);
}

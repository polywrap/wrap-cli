{
  "name": "templates-api-interface",
  "description": "Web3API Interface Example",
  "private": true,
  "version": "0.0.1-prealpha.83",
  "scripts": {
    "build": "npx w3 build",
    "test:env:up": "npx w3 infra up --modules=eth-ens-ipfs",
    "test:env:down": "npx w3 infra down --modules=eth-ens-ipfs",
    "deploy": "npx w3 deploy",
    "test": "yarn test:env:up && yarn deploy && yarn test:env:down"
  },
  "devDependencies": {
    "@web3api/cli": "0.0.1-prealpha.83"
  }
}

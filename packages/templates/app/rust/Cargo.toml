[package]
name = "template_plugin_rs"
version = "0.1.0"
license = "MIT"
description = "Polywrap Rust Plugin Template"
edition = "2021"

include = [
    "src/wrap/*",
    "src/lib.rs"
]

[dependencies]
polywrap = { version = "~0.1.8-beta.2" }
serde = {version = "1.0.145", features = ["derive"]}

[dev-dependencies]

/*
//PSEUDO-CODE Recipe to register subdomain under public subdomain

await registerSubnodeOwnerWithFIFSRegistrar(label, owner, fifsRegistrarAddress)
await setResolver(`${label}.${domain}`, publicResolverAddress, ensAddress)
*/

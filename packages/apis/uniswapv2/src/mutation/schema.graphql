#import { Mutation } into Ethereum from "w3://ens/ethereum.web3api.eth"
#import { ChainId, Currency, Route, Trade, TradeOptions } from "../common/schema.graphql"

type Mutation {
  swap(
    trade: Trade!
    tradeOptions: TradeOptions!
  ): String!
}

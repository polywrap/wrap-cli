#import { Query, ResponseType } into Http from "w3://ens/http.web3api.eth"

type Query {
  catFile(ipfsUrl: String!, cid: String!, catFileOptions: CatFileOptions): Bytes!
}

type CatFileOptions {
  headers: [Http_Header!]
  qs: [Http_UrlParam!]
  offset: Int64
  length: Int64
  responseType: Http_ResponseType
}

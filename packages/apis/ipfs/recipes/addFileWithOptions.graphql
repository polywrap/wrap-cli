mutation {
    addFile(
        ipfsUrl: $ipfsUrl
        fileEntry: {
            name: $name
            data: $data
            options: {
                wrapWithDirectory: true
            }
        }
    )
}